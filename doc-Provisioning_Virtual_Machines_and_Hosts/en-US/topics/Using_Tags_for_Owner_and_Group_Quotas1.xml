<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Lifecycle_and_Automation_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="Using_Tags_for_Owner_and_Group_Quotas1" remap="TID_15562">
	<title>Using Tags for Owner and Group Quotas</title>
	<para> 
		<productname>CloudForms Management Engine</productname> provides tags for enforcing quotas for the owners of virtual machines or instances. Ownership of a virtual machine or instance can be set either during the provisioning process or by using the <guilabel>Configuration Set Ownership</guilabel> button. If a virtual machine or instance has an owner, the value is displayed in the <guilabel>Lifecycle</guilabel> section of the virtual machine or instance summary page.
	</para>
	<para>
		Quota tags can be assigned directly to <emphasis>either</emphasis> a group or owner <emphasis>not</emphasis> to a configuration item. The table below shows the tags for use in quotas.
	</para>
	<informaltable frame="all">
		<tgroup cols="2">
			<thead>
				<row>
					<entry>
						<para>
							Category Display Name
						</para>
						<para>
							(Name)
						</para>
					</entry>
					<entry>
						<para>
							Use
						</para>
						<para />
					</entry>
				</row>
			</thead>
			<tbody>
				<row>
					<entry>
						<para>
							Quota Max CPU
						</para>
						<para>
							(quota_max_cpu)
						</para>
					</entry>
					<entry>
						<para>
							Sets the maximum number of CPUs summed over all virtual machines and instances owned by the group or user.
						</para>
						<para>
							Sample Values: 1, 2, 3, 4, 5, 10, 20, 30, 40, 50
						</para>
					</entry>
				</row>
				<row>
					<entry>
						<para>
							Quota Max Memory
						</para>
						<para>
							(quota_max_memory)
						</para>
					</entry>
					<entry>
						<para>
							Sets the maximum memory summed over all virtual machines and instances owned by the group or user.
						</para>
						<para>
							Sample Values: 1024, 2048, 4096, 8192, 10240, 20480, 40960, 81920 (in MB)
						</para>
					</entry>
				</row>
				<row>
					<entry>
						<para>
							Quota Max Storage
						</para>
						<para>
							(quota_max_storage)
						</para>
					</entry>
					<entry>
						<para>
							Sets the maximum storage summed over virtual machines and instances owned by the group or user.
						</para>
						<para>
							Sample Values: 10, 100, 1000, 20, 200, 40, 400 (in GB)
						</para>
					</entry>
				</row>
			</tbody>
		</tgroup>
	</informaltable>
</section>