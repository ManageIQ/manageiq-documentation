<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Settings_and_Operations_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_create_a_Region" remap="TID_15063">
	<title>Creating a Region</title>
	<para>
		The process of creating a region takes a few minutes. The database is dropped and rebuilt to accommodate the region numbers.
	</para>
	<procedure>
		<title>To Create a Region</title>
		<step>
			<para>
				Start the appliance and log in to the black appliance console with a user name of <parameter>admin</parameter> and the default password. The <guilabel>Appliance Summary Screen</guilabel> displays.
			</para>
		</step>
		<step>
			<para>
				Press <keycap>Enter</keycap> to manually configure settings.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>12</keycap> to <guilabel>Stop EVM Server Processes</guilabel>.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>Y</keycap> to confirm.
			</para>
		</step>
		<step>
			<para>
				The menu reappears after all processes are complete.
			</para>
		</step> 
		<!--<step>
			<para>
				Press <keycap>Enter</keycap> to manually configure settings.
			</para>
		</step>-->
		<step> 
			<!--<para>
				Enter <keycap>7</keycap> to select <guilabel>Setup Database Region</guilabel>.
			</para>-->
			<para>
				Choose <guilabel>Configure Database</guilabel>.
			</para>
			<warning>
				<para>
					Performing this action destroys any existing data and cannot be undone. Back up the existing database before proceeding. By default, new <productname>CloudForms Management Engine</productname> Appliances are assigned region 0. Do not use this number when creating a region as duplicating region numbers can compromise the integrity of the data.
				</para>
			</warning>
		</step>
		<step>
			<para>
				Enter a database region number that has not been used in your environment. Do not enter duplicate region numbers as this can corrupt the data.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>Y</keycap> to confirm.
			</para>
		</step>
		<step>
			<para>
				The menu reappears after all processes are complete.
			</para>
		</step> 
		<!--<step>
			<para>
				Press <keycap>Enter</keycap> to manually configure settings.
			</para>
		</step>-->
		<step>
			<para>
				Enter <keycap>13</keycap> to <guilabel>Start EVM Server Processes</guilabel>.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>Y</keycap> to confirm.
			</para>
		</step>
	</procedure>
	<formalpara>
		<title>Result:</title>
		<para>
			After a region is created, you can create subordinate regions as necessary and set up replication to the top level region.
		</para>
	</formalpara>
</section>