<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Installing_CloudForms_on_Red_Hat_Enterprise_Virtualization.ent">
%BOOK_ENTITIES;
]>
<section id="Configuring_a_Worker_Appliance_for_CloudForms_Management_Engine" remap="TID_24987">
	<title>Configuring a Worker Appliance for CloudForms Management Engine</title>
	<para>
		You can configure a worker appliance through the terminal. These steps demonstrate how to join a worker appliance to an appliance that already has a region configured with a database.
	</para>
	<procedure>
		<title>To Configure a Worker Appliance for <productname>CloudForms Management Engine</productname></title>
		<step>
			<para>
				Start up the appliance and open a terminal console from your virtualization or cloud provider.
			</para>
		</step>
		<step>
			<para>
				The login screen appears:
			</para>
			<screen>
	To administer this appliance, browse to https://192.168.0.40/

	Username:
	  		</screen>
			<para>
				Enter the administration username and password.
			</para>
		</step>
		<step>
			<para>
				The status screen appears and displays details about the CloudForms Management Engine Appliance. Press <userinput> Enter </userinput> to change to the configuration menu.
			</para>
		</step>
		<step>
			<para>
				Select <userinput> 8) Configure Database </userinput> from the menu.
			</para>
		</step>
		<step>
			<para>
				You are prompted to create or fetch a security key. Select the option to fetch the key from the first CFME appliance. All CFME appliances in a multi-region deployment must use the same key.
			</para>
		</step>
		<step>
			<para>
				Choose <userinput> 2) External </userinput> for the database location.
			</para>
		</step>
		<step>
			<para>
				Enter the database hostname or IP address when prompted.
			</para>
		</step>
		<step>
			<para>
				Enter the database name or leave blank for the default (<literal>vmdb_production</literal>).
			</para>
		</step>
		<step>
			<para>
				Enter the database username or leave blank for the default (<literal>root</literal>).
			</para>
		</step>
		<step>
			<para>
				Enter the chosen database user's password.
			</para>
		</step>
		<step>
			<para>
				Confirm the configuration if prompted.
			</para>
		</step>
	</procedure>
	<formalpara>
		<title>Result:</title>
		<para>
			CloudForms Management Engine configures the external database.
		</para>
	</formalpara>
</section>