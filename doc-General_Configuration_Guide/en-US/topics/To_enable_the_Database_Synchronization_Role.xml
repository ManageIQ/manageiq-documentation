<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Settings_and_Operations_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_enable_the_Database_Synchronization_Role" remap="TID_15065">
	<title>Enabling the Database Synchronization Role</title>
	<para>
		When you start the replication worker, all of the information in the subordinate database is sent to the top region (99). The worker also creates triggers so that future changes made to subordinate regions are sent automatically to the top region.
	</para>
	<procedure>
		<title>To Enable the Database Synchronization Role</title>
		<step>
			<para>
				Navigate to <menuchoice><guimenu>Configure</guimenu><guisubmenu>Configuration</guisubmenu></menuchoice>.
			</para>
		</step>
		<step>
			<para>
				Click the <guimenu>Settings</guimenu> accordion and click <guimenu>Zones</guimenu>.
			</para>
		</step>
		<step>
			<para>
				Click the <guimenu>Zone</guimenu> where the server is located and click the server name.
			</para>
			<note>
				<para>
					Only enable database synchronization on subordinate servers with replication worker settings already configured. Do not enable more than one Database Synchronization role per region.
				</para>
			</note>
		</step>
		<step>
			<para>
				Click <guimenu>Server</guimenu>.
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Server Control</guilabel> area, select <guilabel>Database Synchronization</guilabel>.
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>Save</guibutton>.
			</para>
		</step>
	</procedure>
</section>