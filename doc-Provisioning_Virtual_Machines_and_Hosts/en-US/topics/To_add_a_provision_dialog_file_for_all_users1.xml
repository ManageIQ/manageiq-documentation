<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Lifecycle_and_Automation_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_add_a_provision_dialog_file_for_all_users1" remap="TID_15538">
	<title>Adding a Provision Dialog for All Users</title>
	<procedure>
		<title>To Add a Provision Dialog for All Users</title> 
		<!--step>
			<para>
				Login to the <productname>CloudForms Management Engine</productname> console for the <productname>CloudForms Management Engine</productname> server where you want to change the dialog.
			</para>
		</step-->
		<step>
			<para>
				Navigate to <menuchoice><guimenu>Automate</guimenu><guisubmenu>Customization</guisubmenu></menuchoice>.
			</para>
		</step>
		<step>
			<para>
				Click the <guilabel>Provisioning Dialogs</guilabel> accordion.
			</para>
		</step>
		<step>
			<para>
				Click the type of dialog you want to create: <guilabel>Host Provision</guilabel>, <guilabel>VM Provision</guilabel> or <guilabel>VM Migrate</guilabel>.
			</para>
		</step>
		<step>
			<para>
				Select one of the default dialogs.
			</para>
		</step>
		<step>
			<para>
				Click 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1847.png" />
					</imageobject>
				</inlinemediaobject><guilabel>(Configuration)</guilabel>, and then 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1859.png" />
					</imageobject>
				</inlinemediaobject><guilabel>(Copy this Dialog)</guilabel>.
			</para>
		</step>
		<step>
			<para>
				Type a new <guilabel>Name</guilabel> and <guilabel>Description</guilabel> for the dialog.
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Content</guilabel> field,
			</para>
			<itemizedlist>
				<listitem>
					<para>
						To remove a tab from display, change its display value to ignore. By choosing ignore, you not only hide the tab, but also skip any fields on that tab that were required. To show the tab, change the display value to show.
					</para>
				</listitem>
				<listitem>
					<para>
						To hide a field, change its display value from <parameter>edit</parameter> to <parameter>hide</parameter>. To display fields of most data types, use <parameter>edit</parameter>. To display a button, use <parameter>show</parameter>. To set a default value for a field, use <parameter>:default =&gt; defaultvalue</parameter> to the list of parameters for the field. Set the required parameter to either <parameter>true</parameter> or <parameter>false</parameter> based on your needs. Note that if you set required parameter to <parameter>true</parameter>, the field must have a value for the provision request to be submitted.
					</para>
				</listitem>
			</itemizedlist>
		</step>
		<step>
			<para>
				Click <guibutton>Add</guibutton>.
			</para>
		</step>
	</procedure>
	<formalpara>
		<title>Result:</title>
		<para>
			If you are using <guilabel>Provisioning Profiles</guilabel>, you can specify a specific file that holds the customizations. To do this, you must create an instance mapping to this file in the <productname>CloudForms Management Engine</productname> Applications/provisioning/profile/VM provisioning by group class. By default, if you are using provisioning profiles and the group does not have a defined instance, the appropriate default dialog file will be used based on the type of provisioning selected.
		</para>
	</formalpara>
</section>