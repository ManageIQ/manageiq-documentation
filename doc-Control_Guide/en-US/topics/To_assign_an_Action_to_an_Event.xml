<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Control_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_assign_an_Action_to_an_Event" remap="TID_14739">
	<title>Assigning an Action to an Event</title>
	<para>
		This procedure describes how to assign an action to an event.
	</para>
	<procedure>
		<title>To Assign an Action to an Event</title>
		<step>
			<para>
				Navigate to <menuchoice><guimenu>Control</guimenu><guisubmenu>Explorer</guisubmenu></menuchoice>.
			</para>
		</step>
		<step>
			<para>
				Click the <guimenu>Policies</guimenu> accordion, and select the policy you want to assign actions to.
			</para>
		</step>
		<step>
			<para>
				From the <guilabel>Events</guilabel> area, click on the description of the event you want to assign an action to.
			</para>
		</step>
		<step>
			<para>
				Click 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1847.png" />
					</imageobject>
				</inlinemediaobject> (<guilabel>Configuration</guilabel>), 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1881.png" />
					</imageobject>
				</inlinemediaobject> (<guilabel>Edit Actions for this Policy Event</guilabel>).
			</para>
		</step>
		<step>
			<para>
				Select all the appropriate actions from the <guilabel>Available Actions</guilabel> box, inside the <guimenu>Order of Actions if ALL Conditions are True</guimenu>. These are the actions that will take place if the resources meet the Condition of the Policy.
			</para>
			<mediaobject>
				<imageobject>
					<imagedata align="center" fileref="images/1882.png" />
				</imageobject>
			</mediaobject>
			<note>
				<para>
					Each selected action can be executed synchronously or asynchronously; synchronous actions will not start until the previous synchronous action is completed, and asynchronous action allows the next action to start whether or not the first action has completed. Also, at least one <productname>CloudForms Management Engine</productname> server in the <productname>CloudForms Management Engine</productname> zone must have the notifier server role enabled for the trap to be sent.
				</para>
			</note>
		</step>
		<step>
			<para>
				Click the add button ( 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1876.png" />
					</imageobject>
				</inlinemediaobject>), then:
			</para>
			<itemizedlist>
				<listitem>
					<para>
						Click the action, then click 
						<inlinemediaobject>
							<imageobject>
								<imagedata fileref="images/1883.png" />
							</imageobject>
						</inlinemediaobject> (<guiicon>Set selected Actions to Asynchronous</guiicon>) to make it asynchronous.
					</para>
				</listitem>
				<listitem>
					<para>
						Click the action, then click 
						<inlinemediaobject>
							<imageobject>
								<imagedata fileref="images/1884.png" />
							</imageobject>
						</inlinemediaobject> (<guiicon>Set selected Actions to Synchronous</guiicon>) to make it synchronous. If creating a synchronous action, use the up and down arrows to identify in what order you want the actions to run.
					</para>
				</listitem>
			</itemizedlist>
		</step>
		<step>
			<para>
				Select all the actions from the appropriate <guilabel>Available Actions</guilabel> box, inside of the <guimenu>Order of Actions if ANY Conditions are False</guimenu>. These are the actions that take place if the resources do not meet the condition of the policy.
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>Save</guibutton>.
			</para>
		</step>
	</procedure>
</section>