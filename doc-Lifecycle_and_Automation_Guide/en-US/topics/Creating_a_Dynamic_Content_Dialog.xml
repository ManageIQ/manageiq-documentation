<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Lifecycle_and_Automation_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="Creating_a_Dynamic_Content_Dialog" remap="TID_41556">
	<title>Creating a Dynamic Content Dialog</title>
	<para>
		The procedure describes the steps to create a dynamic content dialog.
	</para>
	<procedure>
		<title>To Create a Dynamic Content Dialog</title>
		<step>
			<para>
				Navigate to <menuchoice><guimenu>Automate</guimenu> <guisubmenu>Explorer</guisubmenu></menuchoice>.
			</para>
		</step>
		<step>
			<para>
				From the accordion menu, click <menuchoice><guimenu>DOMAIN</guimenu><guisubmenu>Cloud</guisubmenu><guisubmenu>VM</guisubmenu><guimenuitem>Operations</guimenuitem><guimenuitem>Methods</guimenuitem></menuchoice>.
			</para>
			<note>
				<para>
					DOMAIN must be a user-defined Domain and not the locked ManageIQ Domain. If necessary, you can copy the class from the ManageIQ domain into a custom domain.
				</para>
				<para>
					This example uses the <guilabel>Cloud</guilabel> Namespace but can also use the <guilabel>Infrastructure</guilabel> namespace.
				</para>
			</note>
		</step>
		<step>
			<para>
				Click 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1847.png" />
					</imageobject>
				</inlinemediaobject><guilabel>(Configuration)</guilabel>, then 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/2361.png" />
					</imageobject>
				</inlinemediaobject> <guilabel>(Add a new Instance)</guilabel>.
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Main Info</guilabel> area, enter <guilabel>Name</guilabel> = <replaceable>dynamic_list</replaceable>, replacing <replaceable>dynamic_list</replaceable> with an appropriate name for the method.
			</para>
		</step>
		<step>
			<para>
				Enter a <guilabel>Display Name</guilabel> and <guilabel>Description</guilabel>.
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Fields</guilabel> area, enter <guilabel>Value</guilabel> = <replaceable>dynamic_list</replaceable>. Leave the other fields blank or use the default values.
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>Add</guibutton>.
			</para>
		</step>
		<step>
			<para>
				Navigate to <guilabel>Methods</guilabel> tab.
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Main Info</guilabel> area, enter <guilabel>Name</guilabel> = <replaceable>dynamic_list</replaceable> and populate the <guimenu>Data</guimenu> section with the example automate method below.
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>Add</guibutton>.
			</para>
		</step>
		<step>
			<para>
				Set the automate entry point for the dialog control; use the new instance created in step four. You can create a new domain and copy the method to that domain.
			</para>
		</step>
	</procedure>
	<example>
		<title>Automate Method</title>
		<programlisting>
#  Automate Method

dialog_field = $evm.object

# sort_by: value / description / none
dialog_field["sort_by"] = "value"

# sort_order: ascending / descending
#dialog_field["sort_order"] = "ascending"

# data_type: string / integer
dialog_field["data_type"] = "integer"

# required: true / false
# dialog_field["required"] = "true"

dialog_field["values"] = {1 =&gt; "one", 2 =&gt; "two", 10 =&gt; "ten", 50 =&gt; "fifty"}
dialog_field["default_value"] = 2
</programlisting>
	</example>
</section>