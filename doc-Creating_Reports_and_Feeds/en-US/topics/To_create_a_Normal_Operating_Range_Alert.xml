<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Control_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_create_a_Normal_Operating_Range_Alert" remap="TID_14820">
	<title>Creating a Normal Operating Range Alert</title>
	<para>
		Normal operating range alerts enables you to be notified when the normal operating range is exceeded, or falls below for a period of time from 1 minute to 2 hours. Capacity and utilization must be enabled for normal operating ranges to be calculated. See <ulink url="https://access.redhat.com/documentation/en-US/CloudForms/3.2/html/Settings_and_Operations_Guide/index.html"><citetitle><productname>CloudForms Management Engine</productname> Settings and Operations Guide</citetitle></ulink> for instructions.
	</para>
	<procedure>
		<title>To Create a Normal Operating Range Alert</title>
		<step>
			<para>
				Navigate to <menuchoice><guimenu>Control</guimenu><guisubmenu>Explorer</guisubmenu></menuchoice>.
			</para>
		</step>
		<step>
			<para>
				Click the <guimenu>Alerts</guimenu> accordion, then click 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1847.png" />
					</imageobject>
				</inlinemediaobject> (<guilabel>Configuration</guilabel>), 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/1848.png" />
					</imageobject>
				</inlinemediaobject> (<guilabel>Add a new Alert</guilabel>).
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Info</guilabel> area:
			</para>
			<substeps>
				<step>
					<para>
						Type in a <guilabel>Description</guilabel> for the alert.
					</para>
				</step>
				<step>
					<para>
						From <guilabel>Based On</guilabel>, select <guilabel>VM and Instance</guilabel>.
					</para>
				</step>
				<step>
					<para>
						For <guilabel>What to Evaluate</guilabel>, select <guimenu>Normal Operating Range</guimenu>.
					</para>
				</step>
				<step>
					<para>
						In <guilabel>Notification Frequency</guilabel>, select how often you want to be notified if the performance threshold is reached.
					</para>
				</step>
			</substeps>
		</step>
		<step>
			<para>
				Set the threshold in the <guilabel>Normal Operating Range Parameters</guilabel> area.
			</para>
			<mediaobject>
				<imageobject>
					<imagedata align="center" fileref="images/1976.png" />
				</imageobject>
			</mediaobject>
			<substeps>
				<step>
					<para>
						From <guilabel>Performance Field</guilabel>, select the field to check and whether you want to be notified if the field is exceeded or fell below.
					</para>
				</step>
				<step>
					<para>
						In <guilabel>Field Meets Criteria for</guilabel>, select the amount of time that the threshold requires to be met to trigger the alert.
					</para>
				</step>
			</substeps>
		</step>
		<step>
			<para>
				After setting the parameters, you then select what you want the alert to do. You can send an email, create an SNMP Trap, let the alert show on the timeline, or send a management event to start an automation process. See <xref linkend="To_create_an_Alert" />.
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>Add</guibutton>.
			</para>
		</step>
	</procedure>
</section>