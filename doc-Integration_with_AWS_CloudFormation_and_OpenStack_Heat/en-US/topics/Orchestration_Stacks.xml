<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Integration_with_AWS_CloudFormation_and_OpenStack_Heat.ent">
%BOOK_ENTITIES;
]>
<section id="Orchestration_Stacks" remap="TID_44365">
	<title>Orchestration Stacks</title>
	<para>
		After ordering a service, you can see the progress state of the provisioning process in <menuchoice><guimenu>Services</guimenu><guisubmenu>Requests</guisubmenu></menuchoice>.
	</para>
	<procedure>
		<step>
			<para>
				Initially, the <guilabel>Request State</guilabel> shows <literal>Pending</literal> with its <guilabel>Approval State</guilabel> as <literal>Pending Approval</literal>. 
				<mediaobject>
					<imageobject>
						<imagedata align="center" fileref="images/7177.png" />
					</imageobject>
				</mediaobject>
			</para>
		</step>
		<step>
			<para>
				After the request is <literal>Approved</literal>, the various stages of fulfillment are executed, and reflect accordingly under <guilabel>Request State</guilabel>. 
				<mediaobject>
					<imageobject>
						<imagedata align="center" fileref="images/7178.png" />
					</imageobject>
				</mediaobject>
				<mediaobject>
					<imageobject>
						<imagedata align="center" fileref="images/7179.png" />
					</imageobject>
				</mediaobject>
			</para>
		</step>
		<step>
			<para>
				After the <guilabel>Request State</guilabel> is <literal>Finished</literal>, you can see the stack entry created in <menuchoice><guimenu>Clouds</guimenu><guisubmenu>Stacks</guisubmenu></menuchoice>. In the screen capture below, you can see the <literal>heat-stack</literal> we created from the catalog item ordered from the Service Catalog as shown in the previous section. 
				<mediaobject>
					<imageobject>
						<imagedata align="center" fileref="images/7180.png" />
					</imageobject>
				</mediaobject>
			</para>
		</step>
		<step>
			<para>
				You can click on the stack to see a summary of its properties and relationships, and the instance(s) included in the stack. You can click on the instance(s) to see all instance details. 
				<mediaobject>
					<imageobject>
						<imagedata align="center" fileref="images/7181.png" />
					</imageobject>
				</mediaobject>
			</para>
		</step>
	</procedure>
	<para>
		You have now deployed instances and its associated collection of resources (called a stack) using an orchestration template.
	</para>
</section>