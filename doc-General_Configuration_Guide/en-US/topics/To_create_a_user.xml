<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" 
"http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Settings_and_Operations_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_create_a_user" remap="TID_15007">
	<title>Creating a User</title>
	<procedure>
		<title>To Create a User</title>
		<step>
			<para>
				Navigate to 
<menuchoice><guimenu>Configure</guimenu><guisubmenu>Configuration</guisubmenu></menuchoice>.
			</para>
		</step>
		<step>
			<para>
				Click on the <guilabel>Access Control</guilabel> 
accordion, then click <guilabel>Users</guilabel>.
			</para>
			<mediaobject>
				<imageobject>
					<imagedata align="center" 
fileref="images/2085.png" />
				</imageobject>
			</mediaobject>
		</step>
		<step>
			<para>
				Click 
				<inlinemediaobject>
					<imageobject>
						<imagedata 
fileref="images/1847.png" />
					</imageobject>
				</inlinemediaobject> 
(<guilabel>Configuration</guilabel>), and 
				<inlinemediaobject>
					<imageobject>
						<imagedata 
fileref="images/plus_green.png" />
					</imageobject>
				</inlinemediaobject>(<guilabel>Add a new 
User</guilabel>) to create a user.
			</para>
		</step>
		<step>
			<para>
			  Type in a <guilabel>Full Name</guilabel>, 
<guilabel>Username</guilabel>, <guilabel>Password</guilabel> with confirmation, 
<guilabel>Email Address</guilabel>, and choose a <guilabel>Group</guilabel> for 
the user.
			</para>
			<mediaobject>
				<imageobject>
					<imagedata align="center" 
fileref="images/2086.png" />
				</imageobject>
			</mediaobject>
			<note>
				<para>
					If you are using LDAP, but did not 
enable <guilabel>Get User Groups from LDAP</guilabel> in your server's 
<guilabel>Authentication</guilabel> tab, you will need to define a user. The 
UserID must match exactly the user's name as defined in your directory service. 
Use all lowercase to be sure that the user can be found in the VMDB. For 
example, <replaceable>jdunn@acme.com</replaceable> when using User Principal 
Name, <replaceable>cn=Jack Dunn,ou=users,dc=acme,dc=com</replaceable> when using 
Distinguished Name (<replaceable>CN=&lt;user&gt;</replaceable>), or 
<replaceable>uid=Jack Dunn,ou=users,dc=acme,dc=com</replaceable> when using 
Distinguished Name (<replaceable>UID=&lt;user&gt;</replaceable>). Then, when 
logging in, the user would type either <replaceable>jdunn</replaceable> for User 
Principal Name or <replaceable>Jack Dunn</replaceable> for Distinguished Name. 
If the user is not defined in the VMDB, they will be denied access to 
<productname>CloudForms Management Engine</productname>. The password field will 
not be used. When the user logs in they should use their LDAP password.
				</para>
			</note> 
			<!-- See LDAP Settings.-->
		</step>
		<step>
			<para>
				Select a <guilabel>Group</guilabel>. <!--Role. 
For more information on roles, see Roles.-->
			</para>
		</step>
		<step>
			<para>
				Click <guibutton>Add</guibutton>.
			</para>
		</step>
	</procedure> 
	<!--para>
		Delete a user when they no longer need access to the information 
or functions of the server.
	</para-->
</section>