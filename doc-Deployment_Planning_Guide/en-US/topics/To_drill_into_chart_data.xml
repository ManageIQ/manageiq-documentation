<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Insight_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_drill_into_chart_data" remap="TID_15414">
	<title>Drilling into Chart Data</title>
	<para />
	<procedure>
		<title>To Drill into Chart Data</title>
		<step>
			<para>
				Navigate to the chart you want to get more detail from.
			</para>
		</step>
		<step>
			<para>
				Hover over a data point to see the coordinates.
			</para>
		</step>
		<step>
			<para>
				Click on a data point to open a shortcut menu for the chart. In this example, we can use the shortcut menu to go to the hourly chart or display the virtual machines that were running at the time the data was captured.
			</para>
			<itemizedlist>
				<listitem>
					<para>
						If you are viewing the CPU, Disk, Memory, or Network charts, selecting from the <guimenuitem>Chart</guimenuitem> option will change all of the charts on the page to the new interval selected.
					</para>
				</listitem>
				<listitem>
					<para>
						If you are viewing the CPU, Disk, Memory, or Network charts, selecting from the <guimenuitem>Display</guimenuitem> option will allow you to drill into the virtual machines or Hosts that were running at the time.
					</para>
				</listitem>
				<listitem>
					<para>
						If you are viewing the VM or Hosts chart, the <guimenuitem>Display</guimenuitem> menu will allow you to view running or stopped virtual machines. The time of the data point will be displayed in addition to the virtual machines that apply. From here, click on a virtual machine to go its details.
					</para>
				</listitem>
			</itemizedlist>
		</step>
	</procedure>
</section>