<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Settings_and_Operations_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="To_restore_a_database_from_backup" remap="TID_15069">
	<title>Restoring a Database from a Backup</title>
	<para>
		If a database is corrupt or fails, restore it from a backup.
	</para>
	<procedure>
		<title>To Restore a Database from a Backup</title>
		<step>
			<para>
				Save the database backup file as <filename>/tmp/evm_db.backup</filename>. <productname>CloudForms Management Engine</productname> looks specifically for this file when restoring a database from a backup.
			</para>
		</step>
		<step>
			<para>
				Log in to the black appliance console with a user name of <parameter>admin</parameter> and the default password. The <guilabel>Appliance Summary Screen</guilabel> displays.
			</para>
		</step>
		<step>
			<para>
				Press <keycap>Enter</keycap> to manually configure settings.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>11</keycap> to <guilabel>Stop Server Processes</guilabel>. Stop the process on all servers that connect to this VMDB.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>Y</keycap> to confirm.
			</para>
		</step>
		<step>
			<para>
				After all processes are stopped, press <keycap>Enter</keycap> to return to the menu.
			</para>
		</step>
		<step>
			<para>
				Press <keycap>Enter</keycap> again to manually configure settings.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>6</keycap> to select <guilabel>Restore Database From Backup</guilabel>. If connections are open, the server may still be shutting down. Wait a minute and try again.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>y</keycap> to keep the database backup after restoring from it. Enter <keycap>n</keycap> to delete it.
			</para>
		</step>
		<step>
			<para>
				Press <keycap>Y</keycap> to confirm.
			</para>
		</step>
		<step>
			<para>
				After the backup completes, press <keycap>Enter</keycap> to return to the menu.
			</para>
		</step>
		<step>
			<para>
				Press <keycap>Enter</keycap> again to manually configure settings.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>13</keycap> to <guilabel>Start Server Processes</guilabel>.
			</para>
		</step>
		<step>
			<para>
				Enter <keycap>Y</keycap> to confirm.
			</para>
		</step>
	</procedure>
</section>