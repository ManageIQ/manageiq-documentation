<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Insight_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="Searching_by_Name" remap="TID_15208">
	<title>Searching by Name</title>
	<para>
		A search bar to the right of the taskbar on the <guilabel>Providers</guilabel>, <guilabel>Clusters</guilabel>, <guilabel>Hosts</guilabel>, and <guilabel>Resource Pools</guilabel> pages provide a method to enter names, or parts of names, for searching. Search for items in the following ways:
	</para>
	<orderedlist>
		<listitem>
			<para>
				Type characters that are included in the name. For example, if you type <literal>sp1</literal>, all names that include <literal>sp1</literal> appear such as <literal>Windows2003sp1</literal> and <literal>Sp1clone</literal>.
			</para>
		</listitem>
		<listitem>
			<para>
				Use an asterisk ("<literal>*</literal>") after the desired search term to search for names that begin with specific characters. For example, type <literal>v*</literal> to find all names that begin with the letter <literal>v</literal>.
			</para>
		</listitem>
		<listitem>
			<para>
				Use an asterisk ("<literal>*</literal>") before the desired search term to search for names that end with specific characters. For example, type <literal>*sp2</literal> to find all names that end with <literal>sp2</literal>.
			</para>
		</listitem>
		<listitem>
			<para>
				Erase all characters from the search box to go back to viewing all virtual machines.
			</para>
		</listitem>
	</orderedlist>
	<procedure>
		<title>To Search by a Name</title>
		<step>
			<para>
				Hover over [label]#Infrastructure# or [label]#Clouds# and navigate to either the <guilabel>Providers</guilabel>, <guilabel>Clusters</guilabel>, <guilabel>Hosts</guilabel>, or <guilabel>Resource Pools</guilabel> page.
			</para>
		</step>
		<step>
			<para>
				In the <guilabel>Name Filter</guilabel> bar in the upper right corner of the window, type your criteria. 
				<mediaobject>
					<imageobject>
						<imagedata align="center" fileref="images/2181.png" />
					</imageobject>
				</mediaobject>
			</para>
		</step>
		<step>
			<para>
				Click 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/2135.png" />
					</imageobject>
				</inlinemediaobject> <guibutton>(Search by Name within results)</guibutton>, or press Enter.
			</para>
		</step>
		<step>
			<para>
				Type in another criterion to filter on what is currently displayed.
			</para>
		</step>
		<step>
			<para>
				Click 
				<inlinemediaobject>
					<imageobject>
						<imagedata fileref="images/2135.png" />
					</imageobject>
				</inlinemediaobject> <guibutton>(Search by Name within results)</guibutton>, or press Enter.
			</para>
		</step>
	</procedure>
</section>