<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Settings_and_Operations_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="LDAP_Settings" remap="TID_14980">
	<title>LDAP Settings</title>
	<para>
		If you choose LDAP or LDAPS as your authentication mode, required parameters are exposed under LDAP Settings. Be sure to validate your setting before saving them.
	</para>
	<mediaobject>
		<imageobject>
			<imagedata align="center" fileref="images/2068.png" />
		</imageobject>
	</mediaobject>
	<itemizedlist>
		<listitem>
			<para>
				Use <guilabel>LDAP Host Name</guilabel> to specify the fully qualified domain names of your LDAP servers. CloudForms Management Engine will search each host name in order until it finds one that authenticates the user.
			</para>
		</listitem>
		<listitem>
			<para>
				Use <guilabel>LDAP Port</guilabel> to specify the port for your LDAP server. The default is 389 for LDAP and 636 for LDAPS.
			</para>
		</listitem>
		<listitem>
			<para>
				From the <guilabel>User Type</guilabel> list, select <guimenuitem>User Principal Name</guimenuitem> to type the user name in the format of user@domainname. Select Email Address to login with the users email address. Select Distinguished Name (CN=&lt;user&gt;) or Distinguished Name (UID=&lt;user&gt;) to use just the user name, but be sure to enter the proper <guilabel>User Suffix</guilabel> for either one. Choose the correct Distinguished Name option for your directory service implementation.
			</para>
		</listitem>
		<listitem>
			<para>
				Specify the <guilabel>User Suffix</guilabel>, such as <emphasis>acme.com</emphasis> for <guimenuitem>User Principal Name</guimenuitem> or <emphasis>cn=users,dc=</emphasis><emphasis>acme</emphasis><emphasis>,dc=com</emphasis>for Distinguished Name, in Base DN.
			</para>
		</listitem>
	</itemizedlist>
</section>