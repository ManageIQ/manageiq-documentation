<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../Lifecycle_and_Automation_Guide.ent">
%BOOK_ENTITIES;
]>
<section id="Retiring_Virtual_Machines1" remap="TID_15592">
	<title>Retiring Virtual Machines and Instances</title>
	<para>
		When a virtual machine or instance is no longer required, it can be retired. Once a virtual machine or instance reaches its retirement date, it is immediately shut down and not allowed to restart. If an attempt to restart is made, <productname>CloudForms Management Engine</productname> will shut down the virtual machine or instance.
	</para>
	<para>
		There are three built-in policies involved with retirement.
	</para>
	<itemizedlist>
		<listitem>
			<para>
				If the virtual machine or instance reaches its retirement date, it will be stopped even if it is running.
			</para>
		</listitem>
		<listitem>
			<para>
				If a retired virtual machine or instance is requested to start through <productname>CloudForms Management Engine</productname>, the virtual machine or instance will not be allowed to start.
			</para>
		</listitem>
		<listitem>
			<para>
				If a provider starts a retired virtual machine or instance outside of <productname>CloudForms Management Engine</productname>, the virtual machine or instance will be stopped.
			</para>
		</listitem>
	</itemizedlist>
	<para> 
		<productname>CloudForms Management Engine</productname> provides a number of ways to retire a virtual machine or instance.
	</para>
	<itemizedlist>
		<listitem>
			<para>
				By using the allocated buttons in the <productname>CloudForms Management Engine</productname> console.
			</para>
		</listitem>
		<listitem>
			<para>
				When creating a provision request, a retirement date can be set up.
			</para>
		</listitem>
	</itemizedlist>
</section>